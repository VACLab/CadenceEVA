"use strict"
define([
    "dojo/dom"
], (dom) => {
    return class {

        constructor(_cat, _code) {
            this.cat = _cat;
            this.code = _code;
        }

        //then(_constraint) {
        //}

    }
});
